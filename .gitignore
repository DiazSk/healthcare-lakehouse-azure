# Terraform
*.tfvars
.terraform/
*.tfstate
*.tfstate.backup

# Python
.venv/
__pycache__/

# Local environment / secrets
.env
.env.local
.env.*.local

# IDE / Editor
.idea/

# Logs
*.log

# Marimo
__marimo__/

# Local lakehouse data (rebuilt from public CMS source via pipeline/download.sh)
data/
powerbi/data/
*.parquet

# Local venv
.venv-local/

# graft tool cache
graft/.cache/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Agent working files — instructions, specs and tooling config for Claude, not
# public docs. Kept locally; deliberately not part of the published repository.
CLAUDE.md
context/
.claude/
.ignore
